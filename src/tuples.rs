pub fn run() {
    // let person:(&str, &str, i32) = ("vignesh", "chennai", 30);
    // println!("{} is from {} and is {} yrs old.", person.0, person.1, person.2);

    let t = ("vignesh", "ashokan");
    let tu = (1, t);
    println!("{:#?}", tu);
}